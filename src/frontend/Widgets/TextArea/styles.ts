import { ViewStyle } from 'react-native';

export const styles: Record<'textarea' | 'container', ViewStyle> = {
  textarea: {
    width: '100%',
    paddingLeft: 0,
    paddingTop: 0,
  },
  container: {
    borderRadius: 8,
  },
};
